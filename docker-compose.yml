services:
  app:
    build:
      context: .
      additional_contexts:
        npmcache: ~/.npm
    ports:
      - "4173:4173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 4173
